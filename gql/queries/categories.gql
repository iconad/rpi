query {
  categories(orderBy: [{ field: "id", order: DESC }]) {
    id
    title
    slug
    status
    featured
    created_at
    menu {
      id
      title
    }
    label {
      id
      title
      color
    }
  }
}
