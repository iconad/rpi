query {
  sizes (
    orderBy: [{ field: "created_at", order: DESC }]
  ) {
    id
    region
    type
    content_types
    unit
    price
    portrait
    landscape
    status
    menus {
      id
      title
    }
  }
}
